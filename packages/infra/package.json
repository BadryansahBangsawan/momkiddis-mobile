{
  "name": "@momkiddis/infra",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file .env.local --watch ./alchemy.run.ts --dev --watch",
    "deploy": "bun --env-file .env.local ./alchemy.run.ts",
    "destroy": "bun --env-file .env.local ./alchemy.run.ts --destroy"
  },
  "dependencies": {
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@momkiddis/config": "workspace:*",
    "alchemy": "catalog:",
    "typescript": "catalog:"
  }
}
