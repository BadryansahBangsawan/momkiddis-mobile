import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/sertifikasi")({
	component: () => <div className="p-8 text-center text-muted-foreground">Halaman /sertifikasi — segera hadir</div>,
});
